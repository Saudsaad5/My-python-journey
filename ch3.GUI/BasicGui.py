from tkinter import *

# widgets = GUI elements: buttons, labels, images
# windows = serves as a container to hold or contain widgets

window = Tk() # this will create a window but not display it

window.geometry("240x240") # for the size of the window
window.title("Saud's first GUI") # to change the title
window.config(background="light blue") # to change the background color

window.mainloop() # this will cdisplay the window

